<div class="row">
  <div class="col-4">
    <img class="img-responsive center-block" src="{{user.image}}" style="width: 350px;margin-bottom: 25px;">
  </div>
  <div class="col-8">
    <h1>{{user.name}}</h1>
    <p>{{user.email}}</p>
    <a href="/users/{{user.id}}/edit" class="btn btn-primary">
      Edit
    </a>
  </div>
</div>